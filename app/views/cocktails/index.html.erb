<div class="container">
  <h1>Look at all our tasty Cocktails 👀</h1>

  <ul>
    <% @cocktails.each do |cocktail| %>
      <li>
        <%= link_to cocktail.name, cocktail_path(cocktail) %>
      </li>
    <% end %>
  </ul>

  <h4><strong>Now</strong> that's what I call... a list... of... cocktails?</h4>
</div>
