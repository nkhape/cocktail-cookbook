<div class="container">
  <h1>Look at all our tasty Cocktails 👀</h1>

  <ul>
    <% @cocktails.each do |cocktail| %>
      <li>
        <%= link_to cocktail.name, cocktail_path(cocktail) %>
        <%= link_to cocktail_path(cocktail), method: :delete, data: { confirm: "Are you sure?" } do %>
          <i class="fa fa-trash-alt"></i>
        <% end %>
      </li>
    <% end %>
  </ul>

  <h4><strong>Now</strong> that's what I call... a list... of... cocktails?</h4>

  <p class="btn btn-outline-primary"><%= link_to "Make a new Cocktail", new_cocktail_path %></p>
</div>
